<div>
    <label>TItle</label>
    <!--name is what allows us to say body.name on the server to access it-->
    <input type="text" name="title" value= <%= book.title %> >
    <!--the < % = tells the page to output js as html-->
</div>

<div>
    <label>Author</label>
<select name="author">
<% authors.forEach(author => { %>
    
    <% if(author.id === book.author) { %> 
        <option selected label="<%= author.name %>" value ="<%=author.id %>"></option>
    <% } else { %>
        <option label="<%= author.name %>" value ="<%=author.id %>"></option>
    <% } %>
    
<% }) %>
</select>
<!--book.author IS an id in the book schema-->
</div>

<div>
    <label>Publish Date</label>
    <input type="date" name="publishDate" 
    value="<%= book.publishDate == null ? '' : book.publishDate.toISOString().split('T')[0] %>">
</div>

<div>
    <label>Page Count</label>
    <input type="number" name="pageCount" min="1" 
    value="<%= book.pageCount %>">
</div>

<div>
    <label>Cover</label>
    <input type="file" name="cover">
</div>

<div>
    <label>Description</label> <!--textarea allows enter button-->
    <textarea name="Description"><%= book.description %></textarea>
</div>






